# 15. 要約モード（Summary Mode）仕様

> ✅ **v0.2 完成版（ドラフト段階）**
> このファイルの役割：**将来機能「要約モード」の設計仕様**をドキュメント化。現時点はドラフト（`prompt_summary_v0.1.md`）段階。

---

## 🎯 概要

**要約モード** は、AI Reply の次フェーズとして実装予定の新機能：
- 長文・複雑なバイヤーメッセージを **5要素構造で要約**
- セラーが選択できる **対応パターン（3〜5個）を自動生成**
- それぞれのパターンにメリット/デメリット・推奨度・理由を付与
- セラーは要約を見て方針を決め、その後 AI Reply で返信生成

---

## 🔀 Call-1 / Call-2 の構造

```
【Call-1：要約モード】← このファイルが定義
   入力：商品情報 + チャット履歴 + バイヤー最新メッセージ
   出力：
     ├─ summary（5要素で要約）
     ├─ complexityLevel（low/medium/high）
     ├─ patterns（3〜5個の対応案）
     └─ recommendedPattern（推奨ID）

       ↓ セラーがパターンを選択

【Call-2：返信生成（将来実装）】
   入力：選択されたパターン + その他情報
   出力：従来のAI Reply（jpnLanguage + buyerLanguage）
```

---

## 📐 要約モードのJSON出力スキーマ

```json
{
  "summary": {
    "buyerEmotion": "感情ラベル+強度（1-5）",
    "factsStated": ["バイヤーが述べた客観事実の箇条書き"],
    "requestMade": "明示的な要望",
    "constraints": "制約条件（期限・金額・配送等）",
    "hiddenContext": "翻訳の不自然さ・暗黙の意図"
  },
  "complexityLevel": "low | medium | high",
  "patterns": [
    {
      "id": "A",
      "name": "対応パターン名",
      "description": "セラーが取る行動",
      "merits": ["利点1", "利点2"],
      "demerits": ["欠点1", "欠点2"],
      "recommendation": 1-5,
      "recommendationReason": "なぜその評価か"
    }
  ],
  "recommendedPattern": "パターンID",
  "sellerDecisionNeeded": "セラーが決めるべき核心（1行）"
}
```

---

## 🎚️ COMPLEXITY LEVEL の判定基準

| Level | 判定基準 | AI Reply対応 |
|-------|--------|-----------|
| **low** | 単純な質問、1要件、感情穏やか | 通常AI Reply で十分。要約モード不要 |
| **medium** | 2〜3要件混在、軽度な不満、期限あり | 要約モード推奨 |
| **high** | 長文クレーム、複数要件混在、感情4以上、法的リスク、翻訳で意図不明瞭 | 要約モード必須 |

---

## 📋 PATTERNS生成ルール

- 3〜5個を生成（少なすぎ=選択肢不足、多すぎ=判断疲労）
- 各パターンは **方針が明確に異なる** もの
  - ❌ 表現違いはNG（「返金」「全額返金」「完全返金」は同方針）
  - ✅ 方針違いはOK（「全額返金」「部分返金で継続」「代替品送付」）
- `merits` / `demerits`：最大3個・体言止め・箇条書き
- `recommendation`：1-5の整数（5が最推奨）
- `recommendationReason`：1文

### 判断基準（AIに指示する評価軸）
- eBayポリシー遵守
- 評価（FB）リスク最小化
- セラーの損失最小化

---

## 📝 サンプル出力（`prompt_summary_v0.1.md` 由来）

**ケース：配送傷・返品希望・返送嫌がる**

```json
{
  "summary": {
    "buyerEmotion": "不満（4/5）",
    "factsStated": [
      "商品到着後、シューズのソールに傷があった",
      "写真を添付済み",
      "4日前に受け取った"
    ],
    "requestMade": "全額返金またはディスカウント",
    "constraints": "返送は希望していない（送料負担したくない旨示唆）",
    "hiddenContext": "推測：初回購入でeBayの返品プロセスに不慣れ。悪評を付ける意志は薄く、柔軟な解決を求めている"
  },
  "complexityLevel": "medium",
  "patterns": [
    {
      "id": "A",
      "name": "部分返金（20-30%）で継続所有",
      "description": "商品はバイヤー手元に残し、傷相当分の部分返金で合意する",
      "merits": ["送料負担なし", "評価リスク最小", "迅速解決"],
      "demerits": ["金額交渉の可能性", "傷の度合い次第で不十分"],
      "recommendation": 5,
      "recommendationReason": "バイヤーの示唆に沿う・コスト最小・再発防止にも有効"
    },
    {
      "id": "B",
      "name": "返品・全額返金",
      "description": "商品を返送してもらい全額返金。送料はセラー負担",
      "merits": ["eBay標準手続き", "バイヤー満足度高"],
      "demerits": ["往復送料負担", "商品が傷む可能性", "リサイクル不能"],
      "recommendation": 2,
      "recommendationReason": "コスト大・バイヤー希望と乖離"
    },
    {
      "id": "C",
      "name": "ディスカウントコード提供（次回購入）",
      "description": "今回は現状維持、次回購入時のディスカウントコードを提供",
      "merits": ["今回コスト0", "リピーター化機会"],
      "demerits": ["不満未解決・悪評リスク", "初回購入者には弱い"],
      "recommendation": 1,
      "recommendationReason": "現状不満への直接対応になっていない"
    }
  ],
  "recommendedPattern": "A",
  "sellerDecisionNeeded": "部分返金の金額確定（20%/25%/30%のいずれか）"
}
```

---

## 🚫 禁止事項

- バイヤー向けの**返信文を生成しない**（Call-2の役割）
- eBayポリシー違反の提案（`Encourage off-platform`等）
- 推測と事実の混在（必ず「推測：」と明示）
- 5要素いずれかを空文字で埋めない（該当なしなら「該当なし」と明記）

---

## 🎛️ UI設計（仮）

要約モードのUI構成案：

```
[バイヤーメッセージ]
   ↓
[要約モードボタン「状況整理」]
   ↓
┌────────────────────────────────┐
│ 📊 バイヤー状況サマリ              │
│  感情：不満（4/5）                │
│  要望：全額返金 or ディスカウント  │
│  制約：返送希望しない             │
└────────────────────────────────┘

┌────────────────────────────────┐
│ 💡 対応パターン（3案）             │
│  A. 部分返金で継続 [推奨5/5] ⭐    │
│  B. 返品・全額返金 [推奨2/5]       │
│  C. 次回ディスカウント [推奨1/5]   │
└────────────────────────────────┘

[セラーがパターンを選択]
   ↓
[AI Reply Call-2で返信生成]
```

---

## 🌐 実装に必要な変更

### BayChat フロントエンド
- UI追加（要約モードボタン・結果表示）
- パターン選択UI
- Call-1 / Call-2 の2段階フロー

### Cowatech バックエンド
- 要約モード用 API エンドポイント新設
- 要約モード用プロンプトブロック（BASE/OUTPUT/admin）
- 出力schema（`summary_output`）の登録
- Call-1 → Call-2 の状態管理

### 設計図（このドキュメント群）
- 要約モード用ブロックカード（将来作成）
- Call-1 のペイロード構造仕様
- Call-2 のインプット構造仕様（選択されたパターンをどう渡すか）

---

## ⚠️ 未解決の論点

| # | 論点 | 重要度 |
|---|-----|------|
| — | Call-1 / Call-2 間の状態管理（どこに選択内容を保持するか） | 🔴 高 |
| — | 複雑度自動判定の精度検証 | 🟡 中 |
| — | パターン数（3〜5個）の最適値 | 🟡 中 |
| — | 要約モードの料金プラン（AI Replyオプションに含める？別オプション？） | 🟡 中 |
| — | UIの推奨度表示（星評価・数字・色分け？ ← 信号機色禁止のため） | 🟡 中 |

---

## 🗓️ 実装ロードマップ（未確定）

| フェーズ | 内容 | 状態 |
|---------|------|-----|
| 1 | AI Reply 本番リリース | 進行中 |
| 2 | 要約モード設計図完成 | 🟡 v0.1ドラフトあり |
| 3 | Cowatech実装仕様書作成 | 未着手 |
| 4 | Cowatech実装 | 未着手 |
| 5 | テスト・調整 | 未着手 |
| 6 | 要約モード本番リリース | 未着手 |

---

## 📚 関連ドキュメント

| ドキュメント | 役割 |
|----------|------|
| `prompt_summary_v0.1.md` | 要約モード専用プロンプトのドラフト |
| `prompt_summary_v0.1.html` | ドラフトのHTML版 |
| `prompt_summary_v0.1_preview.html` | プレビュー |
| `block_n3_output_format.md` | 出力schemaとの整合 |
| `services/baychat/ai/CLAUDE.md` | 将来機能タスクリスト |
